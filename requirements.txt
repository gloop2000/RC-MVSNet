# Python 3.7.x required.
# Install torch FIRST — the CUDA build is machine-specific:
#   pip install torch==1.10.1+cu113 torchvision==0.11.2+cu113 \
#     --extra-index-url https://download.pytorch.org/whl/cu113
# (cu113 = correct for TITAN X Pascal, sm_61. Re-check on a new GPU.)

numpy==1.21.2                   # 1.22+ requires Python 3.8
scipy==1.7.3
opencv-python-headless==4.5.5.62
Pillow==8.4.0
matplotlib==3.5.1

plyfile==0.7.4                  # .ply I/O
open3d==0.16.0                  # point cloud fusion  (was MISSING)
trimesh==3.9.42                 # mesh handling       (was MISSING)

# --- keep only if the tensorboard grep found hits ---
tensorboard==2.7.0
protobuf==3.19.3                # 4.x breaks tensorboard 2.7 — do not bump
setuptools==59.5.0              # 59.6+ breaks torch 1.10 tensorboard — do not bump
# ----------------------------------------------------

gdown==4.2.0                    # optional: pretrained checkpoint download